metadata:
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://maxenceng.github.io/tailscale-chart/
    targetRevision: 1.2.2
    chart: easy-tailscale
    helm:
      valueFiles:
        - values.yaml
  values:
    repoURL: https://github.com/maxenceng/home-lab
    targetRevision: HEAD

  destination:
    server: https://kubernetes.default.svc
    namespace: dev